export interface GeneratedContent { primary: string; hook: string; cta: string; hashtags: string[]; variation: string }
export interface VideoScript { hook: string; script: string; cta: string; b_roll: string[]; hashtags: string[] }
export type Plan = 'free' | 'starter' | 'growth' | 'agency'
